# Environment variables for development
# Copy this file to .env.local and modify values as needed

# === APPLICATION ENVIRONMENT ===
NODE_ENV=development
EXPO_PUBLIC_STAGE=dev

# === NETWORK CONFIGURATION ===
# Tu IP local para desarrollo (cambiar según tu red)
LOCAL_IP=192.168.5.107
API_PORT=3000

# === API CONFIGURATION ===
# Development API URLs for different platforms
# Usando variables para mayor flexibilidad
EXPO_PUBLIC_API_URL=http://localhost:${API_PORT}/api
EXPO_PUBLIC_API_URL_ANDROID=http://${LOCAL_IP}:${API_PORT}/api
EXPO_PUBLIC_API_URL_IOS=http://${LOCAL_IP}:${API_PORT}/api

# Para usar en el emulador de Android (alternativa)
# EXPO_PUBLIC_API_URL_ANDROID=http://10.0.2.2:${API_PORT}/api

# Production API URL (only use in production)
# EXPO_PUBLIC_API_URL=https://api.medisupply.com

# === EXPO CONFIGURATION ===
EXPO_DEVTOOLS_LISTEN_ADDRESS=${LOCAL_IP}
EXPO_DEBUG=true
EXPO_PACKAGER_HOST=${LOCAL_IP}
REACT_NATIVE_PACKAGER_HOSTNAME=${LOCAL_IP}

# === AUTHENTICATION ===
# Add any authentication keys or secrets here
# JWT_SECRET=your-jwt-secret-here
# GOOGLE_CLIENT_ID=your-google-client-id
# FACEBOOK_APP_ID=your-facebook-app-id

# === THIRD PARTY SERVICES ===
# Add configuration for external services
# SENTRY_DSN=your-sentry-dsn
# ANALYTICS_KEY=your-analytics-key

# === DATABASE (if using backend services) ===
# DATABASE_URL=postgresql://user:password@localhost:5432/medisupply
# REDIS_URL=redis://localhost:6379

# === FEATURE FLAGS ===
# ENABLE_PUSH_NOTIFICATIONS=true
# ENABLE_BIOMETRIC_AUTH=true
# ENABLE_OFFLINE_MODE=true