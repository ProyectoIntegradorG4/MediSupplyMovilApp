# ================================================
# Variables de Entorno para MediSupply App
# ================================================

# Entorno de desarrollo
EXPO_PUBLIC_STAGE=dev
EXPO_DEBUG=true

# =================================================
# Selección simple de entorno (AWS | local)
# Cambia solo esta variable para alternar entornos
#   aws   → usa EXPO_PUBLIC_GATEWAY_URL (o valor por defecto AWS)
#   local → usa EXPO_PUBLIC_LOCAL_GATEWAY_URL (o valor por defecto local)
# 
# ⚠️ IMPORTANTE: Si defines EXPO_PUBLIC_GATEWAY_URL explícitamente, 
#    se usará esa URL sin importar el valor de EXPO_PUBLIC_ENV
#    Para desarrollo local, COMENTA o ELIMINA EXPO_PUBLIC_GATEWAY_URL
# =================================================
EXPO_PUBLIC_ENV=local

# =================================================
# API Gateway (AWS ALB - Application Load Balancer)
# =================================================
# IMPORTANTE: Todas las peticiones pasan por el API Gateway (AWS ALB)
# El gateway enruta a los microservicios según el path:
#   /api/v1/auth/*       → auth-service:8004
#   /api/v1/users/*      → user-service:8001
#   /api/v1/validate/*   → nit-validation-service:8002
#   /api/v1/audits/*     → audit-service:8003
#   /api/v1/clientes/*   → cliente-service:8013
#
# NOTA: El ALB de AWS usa puerto 80 por defecto (HTTP), no es necesario especificarlo
# Para desarrollo local, puedes cambiar estas URLs a tu IP local (ej: http://192.168.10.5:80)

# AWS - Gateway en ALB (puerto 80 por defecto, no requiere especificar puerto)
# ⚠️ COMENTAR esta línea cuando EXPO_PUBLIC_ENV=local para usar la URL local
# EXPO_PUBLIC_GATEWAY_URL=http://medisupply-alb-656658498.us-east-1.elb.amazonaws.com

# Local - Gateway (NGINX u otro balanceador) usualmente en puerto 80
# Reemplaza la IP por tu red local cuando corresponda
EXPO_PUBLIC_LOCAL_GATEWAY_URL=http://192.168.10.5:80

# Android - AWS ALB
# NOTA: Variables legacy; el nuevo config usa EXPO_PUBLIC_ENV + EXPO_PUBLIC_GATEWAY_URL/EXPO_PUBLIC_LOCAL_GATEWAY_URL
EXPO_PUBLIC_GATEWAY_URL_ANDROID=http://medisupply-alb-656658498.us-east-1.elb.amazonaws.com

# iOS - AWS ALB
# NOTA: Variables legacy; el nuevo config usa EXPO_PUBLIC_ENV + EXPO_PUBLIC_GATEWAY_URL/EXPO_PUBLIC_LOCAL_GATEWAY_URL
EXPO_PUBLIC_GATEWAY_URL_IOS=http://medisupply-alb-656658498.us-east-1.elb.amazonaws.com

# =================================================
# URLs DEPRECADAS (Mantener por compatibilidad)
# =================================================
# Estas URLs ya no se usan, pero se mantienen para compatibilidad
# EXPO_PUBLIC_API_URL=http://192.168.10.5:8001
# EXPO_PUBLIC_AUTH_URL=http://192.168.10.5:8004

# Configuración de red para desarrollo
# Actualiza esta IP con tu IP local actual
LOCAL_IP=192.168.10.5
GATEWAY_PORT=80

# =================================================
# Configuración de Expo Packager (SIEMPRE LOCAL)
# =================================================
# ⚠️ IMPORTANTE: Estas variables SIEMPRE deben apuntar a tu IP local
#    NO cambian según EXPO_PUBLIC_ENV (aws/local)
#    El packager siempre corre en tu máquina de desarrollo
#    Solo el gateway API cambia según EXPO_PUBLIC_ENV
# =================================================
EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.10.5
EXPO_PACKAGER_HOST=192.168.10.5

# Deshabilitar validaciones en línea de Expo (útil para trabajar offline o con problemas de red)
EXPO_NO_UPDATE_CHECK=1
EXPO_NO_TELEMETRY=1
EXPO_OFFLINE=1

